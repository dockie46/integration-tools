# google-tools

Personal Google tooling scratchpad.

## Temporary holding

`gmail-invoice-dataset/` is temporary holding for Keepr-related Gmail invoice tooling, pending a later refactor out of the Keepr app tree.

Do not commit OAuth credentials, `token.json`, or personal invoice PDFs.
