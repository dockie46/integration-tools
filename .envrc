#!/usr/bin/env bash

# With direnv: auto-activates on cd (allow once: `direnv allow`).
# Without direnv: `source .envrc` after `make init`.

if [ ! -d .venv ]; then
  uv sync
fi

source .venv/bin/activate
